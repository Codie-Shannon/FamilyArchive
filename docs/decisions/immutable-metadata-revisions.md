# Immutable metadata revisions

Every accepted Group 11 change appends exactly one immutable before/after revision in the same transaction. Revision rows have no update, delete, revert or rewrite route.
