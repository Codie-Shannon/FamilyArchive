# Admin photo intake boundary

Group 04 validates one temporary Owner-selected photo, records technical facts and plans an `archive_quarantine` relative path. No application service retains or mutates media bytes.
