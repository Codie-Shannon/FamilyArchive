# Family Archive Group 06 Evidence

This evidence set closes Group 06: exact SHA-256 duplicate candidate detection.

The screenshots prove that exact matches create pending manual-review candidates only. They do not approve, merge, replace, download, promote, resolve or delete any source or archive record. Detection remains database-fact-only and storage-neutral.

## Required evidence

Exactly seven approved PNG screenshots are included. The finalization runner installs them under:

`docs/screenshot-groups/group-06/`

The runner validates the evidence, reruns focused and full regression tests, builds production assets, performs dependency/security/privacy/Git checks, commits with the approved Group 06 Pack 2 subject, pushes `main`, and verifies a synchronized clean repository.
