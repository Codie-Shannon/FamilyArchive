# Family Archive Group 07 Evidence

This evidence set closes Group 07: controlled manual duplicate-review decisions.

The screenshots prove that a verified Owner can classify one exact duplicate candidate at a time as `confirmed_duplicate`, `alternate_source`, `related_but_distinct` or `not_duplicate`. Decisions are transaction-backed and auditable, corrections append immutable history, and retained source bytes remain untouched.

No screenshot or Group 07 route adds automatic deletion, cleanup, download, merge, replacement, preferred-scan selection, archive attachment, approval, original promotion or derivative generation.

## Required evidence

Exactly seven approved PNG screenshots are included under:

`docs/screenshot-groups/group-07/`

The Pack 2 runner verifies the exact approved image hashes, PNG signatures, dimensions and evidence filenames. It then reruns Group 07 focused tests, the complete regression suite, static analysis, the production build, dependency audits, privacy and Git hygiene checks, commits with the approved Group 07 Pack 2 subject, pushes `main`, and proves synchronized clean closure.
