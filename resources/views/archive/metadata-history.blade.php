<x-layouts::app :title="'Metadata history - '.$mediaItem->archive_id"><div class="mx-auto flex w-full max-w-5xl flex-1 flex-col gap-6 p-4 md:p-8"><header><a href="{{ route('archive.photos.show',$mediaItem) }}" class="text-sm text-emerald-300">← Back to photo</a><p class="mt-5 text-xs uppercase text-emerald-300">{{ $mediaItem->archive_id }}</p><h1 class="text-3xl font-semibold text-white">Metadata revision history</h1><p class="text-zinc-400">Current revision {{ $mediaItem->metadata_revision }} · {{ $revisions->count() }} immutable revision records</p></header><section class="space-y-4">@forelse($revisions as $revision)<a href="{{ route('archive.photos.metadata.history.show',[$mediaItem,$revision]) }}" class="block rounded-xl border border-zinc-700 bg-zinc-900 p-5"><div class="flex justify-between"><div><h2 class="text-lg font-semibold text-white">Revision {{ $revision->revision_number }}</h2><p class="text-sm text-zinc-400">{{ $revision->actor->name }} · {{ $revision->created_at->format('j M Y, g:i a') }}</p></div><span class="text-sm text-emerald-300">{{ implode(', ',$revision->changed_fields) }}</span></div><p class="mt-3 text-zinc-200">{{ $revision->change_reason }}</p></a>@empty<p>No metadata revisions recorded.</p>@endforelse</section><div class="rounded-xl border border-amber-700 p-4 text-amber-100">Append-only audit evidence: no revert, delete, rewrite, download or storage action exists.</div></div></x-layouts::app>
