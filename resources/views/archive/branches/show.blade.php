<x-layouts::app title="Reviewed Family Branch">
    <main class="mx-auto max-w-6xl space-y-6 p-6">
        <x-archive-navigation />
        @if(session('status'))<div class="rounded-lg border border-emerald-300 bg-emerald-50 p-4 text-emerald-800">{{ session('status') }}</div>@endif
        <header class="flex flex-wrap items-end justify-between gap-4">
            <div><p class="font-mono text-xs text-zinc-500">{{ $branch->branch_id }}</p><h1 class="text-3xl font-semibold text-zinc-950 dark:text-white">{{ $branchPresenter->browseName($branch) }}</h1><p class="mt-2 text-zinc-600 dark:text-zinc-300">{{ $branchPresenter->browseDescription($branch) }}</p></div>
            @if($canCurate)<a href="{{ route('archive.branches.edit', $branch) }}" class="rounded-lg bg-emerald-500 px-4 py-2 font-semibold text-black">Review branch</a>@endif
        </header>
        @if($branch->is_sensitive)<aside class="rounded-xl border border-amber-300 bg-amber-50 p-5 text-amber-950 dark:border-amber-800 dark:bg-amber-950/30 dark:text-amber-100"><strong>Sensitive branch boundary</strong><p class="mt-1 text-sm">Branch description, people and provenance details are withheld from archive browsing.</p></aside>@endif
        <section class="grid gap-4 md:grid-cols-3">
            <article class="rounded-xl border border-zinc-200 bg-white p-5 shadow-sm dark:border-zinc-700 dark:bg-zinc-900"><p class="text-xs uppercase text-zinc-500">Confidence</p><p class="mt-2 font-semibold">{{ $branch->is_sensitive ? 'Withheld' : str($branch->confidence->value)->headline() }}</p></article>
            <article class="rounded-xl border border-zinc-200 bg-white p-5 shadow-sm dark:border-zinc-700 dark:bg-zinc-900"><p class="text-xs uppercase text-zinc-500">Reviewed people</p><p class="mt-2 font-semibold">{{ $branch->is_sensitive ? 'Withheld' : $branch->people->count() }}</p></article>
            <article class="rounded-xl border border-zinc-200 bg-white p-5 shadow-sm dark:border-zinc-700 dark:bg-zinc-900"><p class="text-xs uppercase text-zinc-500">Reviewed status</p><p class="mt-2 font-semibold">Accepted</p></article>
        </section>
        @if(!$branch->is_sensitive)
            <section class="rounded-xl border border-zinc-200 bg-white p-6 shadow-sm dark:border-zinc-700 dark:bg-zinc-900"><h2 class="text-xl font-semibold">Reviewed people in this branch</h2><div class="mt-4 grid gap-3 md:grid-cols-2">@forelse($branch->people as $person)<a href="{{ route('archive.people.show', $person) }}" class="rounded-lg border border-zinc-200 bg-zinc-50 p-4 dark:border-zinc-700 dark:bg-zinc-950"><strong>{{ $personPresenter->browseName($person) }}</strong><p class="font-mono text-xs text-zinc-500">{{ $person->person_id }}</p><p class="mt-1 text-sm text-zinc-500">{{ $personPresenter->lifeDates($person) }}</p></a>@empty<p class="text-zinc-500 md:col-span-2">No accepted people are assigned to this branch.</p>@endforelse</div></section>
        @endif
        @if($canCurate)<section class="rounded-xl border border-zinc-200 bg-white p-6 shadow-sm dark:border-zinc-700 dark:bg-zinc-900">
            <h2 class="text-xl font-semibold">Branch provenance</h2>
            <div class="mt-4 grid gap-3 md:grid-cols-2">@forelse($branch->provenanceLinks as $link)<a href="{{ route('archive.sources.show', $link->sourceCollection) }}" class="rounded-lg border border-zinc-200 bg-zinc-50 p-4 dark:border-zinc-700 dark:bg-zinc-950"><strong>{{ $branch->is_sensitive ? 'Reviewed source withheld' : $link->sourceCollection->name }}</strong>@if(!$branch->is_sensitive)<p class="font-mono text-xs text-zinc-500">{{ $link->sourceCollection->source_id }}@if($link->scanBatch) · {{ $link->scanBatch->scan_batch_id }}@endif</p><p class="mt-1 text-sm text-zinc-500">{{ $link->note }}</p>@endif</a>@empty<p class="text-zinc-500 md:col-span-2">No reviewed branch source is linked yet.</p>@endforelse</div>
            @if($sources->isNotEmpty())
                <form method="POST" action="{{ route('archive.branches.provenance.store', $branch) }}" class="mt-5 grid gap-3 border-t border-zinc-200 pt-5 dark:border-zinc-700 md:grid-cols-2">@csrf<input type="hidden" name="expected_metadata_revision" value="{{ $branch->metadata_revision }}"><select name="source_collection_id" required class="rounded-lg border border-zinc-300 bg-white p-3 dark:border-zinc-700 dark:bg-zinc-950"><option value="">Select source</option>@foreach($sources as $source)<option value="{{ $source->id }}">{{ $source->source_id }} · {{ $source->name }}</option>@endforeach</select><select name="scan_batch_id" class="rounded-lg border border-zinc-300 bg-white p-3 dark:border-zinc-700 dark:bg-zinc-950"><option value="">No scan batch</option>@foreach($sources as $source)@foreach($source->scanBatches as $batch)<option value="{{ $batch->id }}">{{ $source->source_id }} · {{ $batch->label }}</option>@endforeach @endforeach</select><textarea name="note" rows="2" placeholder="Reviewed provenance note" class="rounded-lg border border-zinc-300 bg-white p-3 dark:border-zinc-700 dark:bg-zinc-950"></textarea><textarea name="change_reason" required rows="2" placeholder="Reason for attaching this source" class="rounded-lg border border-zinc-300 bg-white p-3 dark:border-zinc-700 dark:bg-zinc-950"></textarea><button class="w-fit rounded-lg bg-emerald-500 px-4 py-2 font-semibold text-black">Attach source</button></form>
            @endif
        </section>
        <section class="rounded-xl border border-zinc-200 bg-white p-6 shadow-sm dark:border-zinc-700 dark:bg-zinc-900"><h2 class="text-xl font-semibold">Immutable revision evidence</h2><div class="mt-4 space-y-3">@foreach($branch->revisions as $revision)<article class="rounded-lg bg-zinc-100 p-4 text-sm dark:bg-zinc-950"><strong>Revision {{ $revision->revision_number }}</strong><p class="text-zinc-500">{{ implode(', ', $revision->changed_fields) }} · {{ $revision->actor->name }}</p><p class="mt-1">{{ $revision->change_reason }}</p></article>@endforeach</div></section>@endif
    </main>
</x-layouts::app>
