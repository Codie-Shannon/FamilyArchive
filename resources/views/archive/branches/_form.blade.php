@php($editing = isset($branch))
@php($currentReviewState = old('review_state', $editing ? $branch->review_state->value : 'accepted'))
@php($currentConfidence = old('confidence', $editing ? $branch->confidence->value : 'unknown'))
<div class="grid gap-5 md:grid-cols-2">
    <label class="space-y-2 md:col-span-2"><span class="text-sm font-medium">Branch name</span><input name="name" required maxlength="160" value="{{ old('name', $editing ? $branch->name : '') }}" class="w-full rounded-lg border border-zinc-300 bg-white p-3 dark:border-zinc-700 dark:bg-zinc-950"></label>
    <label class="space-y-2 md:col-span-2"><span class="text-sm font-medium">Reviewed description</span><textarea name="description" rows="4" class="w-full rounded-lg border border-zinc-300 bg-white p-3 dark:border-zinc-700 dark:bg-zinc-950">{{ old('description', $editing ? $branch->description : '') }}</textarea></label>
    <label class="space-y-2"><span class="text-sm font-medium">Sensitive branch</span><select name="is_sensitive" class="w-full rounded-lg border border-zinc-300 bg-white p-3 dark:border-zinc-700 dark:bg-zinc-950"><option value="0" @selected((string) old('is_sensitive', $editing ? (int) $branch->is_sensitive : 0) === '0')>No — browse reviewed facts</option><option value="1" @selected((string) old('is_sensitive', $editing ? (int) $branch->is_sensitive : 0) === '1')>Yes — redact browse facts</option></select></label>
    <label class="space-y-2"><span class="text-sm font-medium">Confidence</span><select name="confidence" class="w-full rounded-lg border border-zinc-300 bg-white p-3 dark:border-zinc-700 dark:bg-zinc-950">@foreach(\App\Domain\Media\Enums\StructuredDateConfidence::cases() as $case)<option value="{{ $case->value }}" @selected($currentConfidence === $case->value)>{{ str($case->value)->headline() }}</option>@endforeach</select></label>
    <label class="space-y-2 md:col-span-2"><span class="text-sm font-medium">Source note</span><textarea name="source_note" rows="3" class="w-full rounded-lg border border-zinc-300 bg-white p-3 dark:border-zinc-700 dark:bg-zinc-950">{{ old('source_note', $editing ? $branch->source_note : '') }}</textarea></label>
    <label class="space-y-2"><span class="text-sm font-medium">Review state</span><select name="review_state" class="w-full rounded-lg border border-zinc-300 bg-white p-3 dark:border-zinc-700 dark:bg-zinc-950">@foreach(\App\Domain\Knowledge\Enums\KnowledgeReviewState::cases() as $case)<option value="{{ $case->value }}" @selected($currentReviewState === $case->value)>{{ str($case->value)->headline() }}</option>@endforeach</select></label>
    <label class="space-y-2 md:col-span-2"><span class="text-sm font-medium">Review reason</span><textarea name="review_reason" required rows="3" class="w-full rounded-lg border border-zinc-300 bg-white p-3 dark:border-zinc-700 dark:bg-zinc-950">{{ old('review_reason', $editing ? $branch->review_reason : '') }}</textarea></label>
</div>
@if($errors->any())<div class="mt-5 rounded-lg border border-red-300 bg-red-50 p-4 text-sm text-red-800">@foreach($errors->all() as $error)<p>{{ $error }}</p>@endforeach</div>@endif
