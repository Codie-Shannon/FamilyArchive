<x-layouts::app :title="__('Incoming Upload Detail')"><div class="mx-auto flex w-full max-w-7xl flex-1 flex-col gap-6 p-4 md:p-8">
@if(session('created_upload'))<section class="rounded-xl border border-emerald-700 bg-emerald-950/30 p-5 text-emerald-200">Valid photo record created. Temporary source bytes were not retained.</section>@endif
<header><p class="text-sm text-emerald-300">Read-only IncomingUpload</p><h1 class="text-3xl font-semibold text-white">{{ $upload['upload_id'] }}</h1></header>
<section class="grid gap-4 lg:grid-cols-2"><article class="rounded-xl border border-zinc-700 bg-zinc-900 p-6"><h2 class="text-xl font-semibold text-white">Technical facts</h2><dl class="mt-5 grid grid-cols-2 gap-4 text-sm"><dt class="text-zinc-500">Original filename (untrusted)</dt><dd class="text-white">{{ $upload['original_filename'] }}</dd><dt class="text-zinc-500">Detected MIME</dt><dd>{{ $upload['mime_type'] }}</dd><dt class="text-zinc-500">Extension</dt><dd>{{ $upload['extension'] }}</dd><dt class="text-zinc-500">Size</dt><dd>{{ number_format($upload['size_bytes']) }} bytes</dd><dt class="text-zinc-500">Dimensions</dt><dd>{{ $upload['dimensions'] }}</dd></dl></article>
<article class="rounded-xl border border-zinc-700 bg-zinc-900 p-6"><h2 class="text-xl font-semibold text-white">Planned quarantine location</h2><p class="mt-4 text-zinc-400">Logical disk</p><code class="text-emerald-300">{{ $upload['logical_disk'] }}</code><p class="mt-4 text-zinc-400">Relative path only</p><code class="break-all text-white">{{ $upload['incoming_path'] }}</code></article></section>
<section class="rounded-xl border border-zinc-700 bg-zinc-900 p-6"><h2 class="text-xl font-semibold text-white">Initial record state</h2><div class="mt-5 grid gap-3 md:grid-cols-3"><div class="rounded bg-zinc-800 p-4">{{ $upload['processing_status'] }}</div><div class="rounded bg-zinc-800 p-4">{{ $upload['review_status'] }}</div><div class="rounded bg-zinc-800 p-4">{{ $upload['duplicate_status'] }}</div></div><div class="mt-5 grid gap-3 md:grid-cols-4 text-sm"><p>SHA-256: <strong>{{ $upload['sha256'] ?? 'null' }}</strong></p><p>Perceptual hash: <strong>{{ $upload['perceptual_hash'] ?? 'null' }}</strong></p><p>Archive link: <strong>{{ $upload['media_item_id'] ?? 'null' }}</strong></p><p class="text-emerald-300">source_file_retained=false</p></div><p class="mt-5 text-zinc-400">No approve, reject, retry, copy, move, download, replace or delete action is available.</p></section>
</div></x-layouts::app>