<x-layouts::app :title="__('Incoming Upload Queue')"><div class="mx-auto flex w-full max-w-7xl flex-1 flex-col gap-6 p-4 md:p-8">
<header><p class="text-sm text-emerald-300">Owner-only / read-only</p><h1 class="text-3xl font-semibold text-white">Incoming Upload Queue</h1><p class="mt-2 text-zinc-400">Planned records only. No source bytes retained and no mutation actions.</p></header>
<section class="overflow-x-auto rounded-xl border border-zinc-700 bg-zinc-900"><table class="w-full min-w-[1000px] text-left text-sm"><thead class="bg-zinc-800 text-zinc-400"><tr><th class="p-4">Upload ID</th><th>Technical facts</th><th>Planned path</th><th>Statuses</th><th>Retention</th></tr></thead><tbody>@forelse($rows as $row)<tr class="border-t border-zinc-700"><td class="p-4"><a class="text-emerald-300" href="{{ route('admin.photo-intake.show',$row['id']) }}">{{ $row['upload_id'] }}</a></td><td>{{ $row['mime_type'] }} / {{ $row['dimensions'] }}</td><td><code>{{ $row['incoming_path'] }}</code></td><td>{{ $row['processing_status'] }} · {{ $row['review_status'] }} · {{ $row['duplicate_status'] }}</td><td class="text-emerald-300">Source bytes not retained</td></tr>@empty<tr><td colspan="5" class="p-8 text-zinc-400">No incoming records.</td></tr>@endforelse</tbody></table></section>
</div></x-layouts::app>